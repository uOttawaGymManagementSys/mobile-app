# When testing on a real phone, localhost won’t work.
# use your machine’s LAN IP instead in API_BASE_URL, e.g.
# API_BASE_URL=http://192.168.0.15:3000/api
# localhost is fine if you’re using an emulator or Expo running on the same machine

API_BASE_URL=http://localhost:3000/api